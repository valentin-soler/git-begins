Cloner un repository :
git clone <repo>

Commandes de push:

git add <fichier a inclure, * inclus tout>
git commit -m <message>
git push

Commande de pull:

git pull

Commande pour crée une branche depuis l'origine et basculer dans cette dernière :

git checkout -b <nom de la branche> (cela la crée localement)
git push origin <nom de la branche> (cela l'envoie sur GitHub)

Crée juste une branche : 
git branch <nom de la branche>

Basculer dans une branche 

git checkoout <nom de la branche>
